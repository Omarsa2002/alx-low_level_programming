#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"

/**
 *add - prints the type
 *of the number
 *@a: an integer char
 *@b: an integer
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
